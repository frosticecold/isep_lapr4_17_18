package pt.isep.nsheets.shared.lapr4.blue.s1.lang.n1090657.visualbasic.compiler;


import com.google.gwt.junit.client.GWTTestCase;
import org.junit.Test;

import pt.isep.nsheets.shared.core.Cell;
import pt.isep.nsheets.shared.core.Spreadsheet;
import pt.isep.nsheets.shared.core.Workbook;
import pt.isep.nsheets.shared.core.formula.compiler.ExpressionCompiler;
import pt.isep.nsheets.shared.core.formula.compiler.FormulaCompilationException;
import pt.isep.nsheets.shared.lapr4.blue.s1.lang.n1090657.macrolanguage.compiler.MacroCompilerManager;

public class VisualBasicExpressionCompilerTest{

	
	

	
	/*
	@Test
	public void oneLineReference() {
            	String[][][] contents = { { // first spreadsheet
		{ "1", "1", "1", "1", "1", "1", "1" }, { "8", "=1+7", "6", "5", "4", "3", "2" },
		{ "1", "2", "3", "4", "5", "6", "7" }, } };
	Workbook workbook = new Workbook(contents);
		ExpressionCompiler compiler = MacroCompilerManager.getInstance().getCompiler("VisualBasic");
		Spreadsheet ss =workbook.getSpreadsheet(0);
		Cell cell =ss.getCell(0, 0);
		String macrotext = "A1+B1";
		try {
			compiler.compile(cell, macrotext);
		} catch (FormulaCompilationException e) {
		}
	}
*/
}
